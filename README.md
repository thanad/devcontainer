# Devcontainer Project

This repository contains a simple development container setup.

## Structure

- `Dockerfile` - builds the devcontainer image.
- `package.json` - npm configuration.
- `nginx/default.conf` - nginx configuration.
- `src/index.js` - application entry point.

## Usage

Build and run the container per project documentation.
